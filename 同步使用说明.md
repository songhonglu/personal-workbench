# 工作台多端同步使用说明

> 目标：手机和电脑打开工作台，看到并写入**同一份数据**。

## 一、数据模型

| 场景 | 数据位置 | 是否共享 |
|------|---------|---------|
| 同一浏览器打开桌面版 / 移动版 | 浏览器 `localStorage`（key：`workbench-v1`） | ✅ 自动共享，无需任何配置 |
| 不同设备（手机 ↔ 电脑） | 各自浏览器的 localStorage，本地无法互通 | 需启用下面的同步服务 |

未开启同步时，所有数据仍只存本机，功能不受影响；左下角「导出备份」随时可导出 JSON。

## 二、启用跨设备同步（3 步）

**1. 电脑上启动同步服务（零依赖，Node 14+）**

```bash
cd 个人工作台
node sync-server.js
```

启动后终端会打印局域网地址，例如 `http://10.0.72.163:8788`。

**2. 工作台里填写同步配置（电脑版 + 手机版都要填）**

- 打开工作台 → 侧栏左下角「**同步**」按钮
- 后端地址：电脑上填 `http://127.0.0.1:8788`；手机上填终端打印的局域网地址
- 同步令牌：两端填**同一个**，默认 `song-workbench-2026`（建议改，见下）
- 点「保存并同步」→ 状态显示「已连接」

**3. 验证**

在电脑上记一笔，手机上打开工作台或点一下「同步」按钮，数据即出现。

## 三、同步机制

- 任何一次新增/修改/勾选后 **1 秒内自动推送**到后端
- 打开页面时自动拉取云端最新数据；侧栏「同步」按钮可手动双向同步
- 云端按 `rev` 版本号判断新旧，只接受更新的版本
- 数据以云端为准：**离线期间的本地改动可能被云端覆盖**，恢复网络后先点一次「同步」

## 四、注意事项

- 手机和电脑必须连**同一个 Wi-Fi**；手机浏览器打开的是工作台 HTML 文件，后端地址填局域网 IP
- 电脑合盖 / 关机后服务停止，手机端读写会失败（数据仍在手机本地，不丢）
- 修改令牌：编辑 `sync-server.js` 顶部 `TOKEN`，或在终端用环境变量启动 `SYNC_TOKEN=你的密钥 node sync-server.js`，两端重新填一次即可
- 云端数据落在同目录 `workbench-sync-data.json`，定期复制它即等于备份
- 想开机自动启动服务：`crontab` 加 `@reboot` 或用 launchd / pm2 托管（可选）

## 五、常见问题

| 现象 | 处理 |
|------|------|
| 手机填地址后「同步失败」 | 检查是否同一 Wi-Fi；电脑防火墙是否放行 8788 端口 |
| 端口被占用 | 换端口启动：`node sync-server.js 9000`，两端地址同步改 |
| 状态一直「未启用同步」 | 后端地址和令牌两个输入框都必须非空 |
